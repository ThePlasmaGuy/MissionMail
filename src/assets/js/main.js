/* Default Setup Scripts for default Render Process */

var google = electron.remote.getGlobal('google'); // Google Library
var auth = electron.remote.getGlobal('auth'); // GoogleAPI Auth Function
var missionaries = electron.remote.getGlobal('missionaries'); // Missionary Data
var companionships = electron.remote.getGlobal('companionships'); // Companionship Data
var zoneLeaders = electron.remote.getGlobal('zoneLeaders'); // Zone Leader Data
